# Application dependencies

require:
    - play
    - play -> crud
    - orientdb -> orientdb 0.1.3

repositories:
    - localModules:
        type: local
        artifact: "${application.path}/../../dist/[module]-[revision].zip"
        contains:
            - orientdb -> *